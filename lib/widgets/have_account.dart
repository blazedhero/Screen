// ignore_for_file: public_member_api_docs, sort_constructors_first
import 'package:flutter/material.dart';

class HavingWidget extends StatelessWidget {
  bool haveAccount;

  HavingWidget({
    Key? key,
    required this.haveAccount,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
